/** Mono uppercase label — column headers, section kicks, metadata keys. */
export interface LabelProps { style?: React.CSSProperties; children?: React.ReactNode; }
export function Label(props: LabelProps): JSX.Element;