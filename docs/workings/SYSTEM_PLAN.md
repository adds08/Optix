# STInventory — System Plan

**This file moved. The plan lives at [`../../SYSTEM_PLAN.md`](../../SYSTEM_PLAN.md), at the
repository root, beside `CLAUDE.md` and `AGENTS.md`.**

There were two copies of this document for a month. Both were tracked, both were edited, and
every pointer in the repository — `AGENTS.md`, `docs/README.md` — named **this** one as
canonical while the root copy was the one people actually changed. By 2026-08-24 the root
copy was 140 lines ahead, including the whole of Phase 5 and the audit findings; this one
still described a release date that had already moved.

The section structure was identical, so nothing is lost by collapsing them. The old content
is in git history if it is ever wanted.

Kept as a pointer rather than deleted, because changelogs and code comments cite this path.
