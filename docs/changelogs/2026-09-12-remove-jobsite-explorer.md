# Keep the existing Jobsites layout

Removed the local `/jobsites/explorer` experiment at the user's request. Its split view did not provide the useful jobsite context they wanted. `/jobsites` remains the chosen layout.

## What changed

Deleted the Explorer page and marked its screen-map entry stale. The earlier creation entry remains as history. No changes were made to the existing Jobsites page or to application data.

## Verified

Confirmed the Explorer page is absent and the existing Jobsites page remains present. No other application files reference the removed route.

## Where it is

Local working tree only; not deployed.
